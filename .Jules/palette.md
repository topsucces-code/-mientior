# Palette's Journal - Critical Learnings

## 2024-05-23 - Initial Exploration
**Learning:** This codebase uses a standard `cn` utility for class merging and `cva` for component variants, typical of shadcn/ui.
**Action:** When modifying components, respect the `cva` structure and use `cn` for any conditional class additions.
