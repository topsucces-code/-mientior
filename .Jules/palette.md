## 2026-01-16 - Icon-only buttons accessibility
**Learning:** Icon-only buttons often lack context for both screen readers and visual users.
**Action:** Always pair `aria-label` with `Tooltip` for these actions.
